# SUBIT-∞

## Формальна специфікація ієрархії SUBIT-64 ⊂ SUBIT⁺ ⊂ SUBIT-TOPOS ⊂ SUBIT_∞

**Версія 5.2**

---

## Анотація

SUBIT-∞ — формальна нотаційна система для опису самореферентних динамічних систем: систем, у яких правило еволюції є частиною самого стану, а не зовнішнім параметром. Специфікація побудована як строга кумулятивна ієрархія з чотирьох рівнів, кожен з яких є самодостатньою, завершеною системою і додає до попереднього рівно один новий шар структури:

```
SUBIT-64  ⊂  SUBIT⁺  ⊂  SUBIT-TOPOS  ⊂  SUBIT_∞
```

**SUBIT-64** фіксує статичний алфавіт із 64 архетипних станів. **SUBIT⁺** додає внутрішнє правило еволюції та оператор, що змінює і стан, і правило. **SUBIT-TOPOS** підносить це до категорії з класифікатором стабільності, типовою системою та модальною логікою. **SUBIT_∞** знімає обмеження скінченності, розширюючи простір станів до рекурсивної коіндуктивної структури дерев із мітками, у якій категорія "хаотичної" динаміки вперше стає математично змістовною.

У версії 5.2 додано розділ 18 — канонічну побудову функції P(τ), що з'єднує довільну спостережену траєкторію з множиною, яку класифікує Ω (закриваючи приховану передумову означення семантичної ультраметрики), і показує, чому найпростіший напрошуваний спосіб такої побудови (P(τ):=ω-границя) вироджує класифікатор у константу. Це, своєю чергою, додає до протоколу застосування (розділ 19) явний Крок 0 — вимогу обґрунтування кодування неперервних емпіричних вимірів у скінченний алфавіт, за аналогією з породжувальними розбиттями в символічній динаміці.

Специфікація завершується нормативним розділом про епістемологічний статус системи: SUBIT-∞ є інструментом формулювання й структурування гіпотез, а не емпіричною теорією.

---

## Зміст

**Частина I. SUBIT-64 — алфавіт станів**
1. Алфавіт примітивів
2. Простір станів S₀

**Частина II. SUBIT⁺ — скінченна самореферентна динаміка**
3. Простір правил і розширений стан
4. Оператор еволюції та ієрархія рівнів самореференції
5. Траєкторії у скінченному просторі

**Частина III. SUBIT-TOPOS — категорія, класифікатор, типи**
6. Класифікатор стабільності Ω
7. Алгебра Ω
8. Категорія SUBIT
9. SUBIT як proto-topos
10. Динамічна теорія типів
11. Модальна логіка
12. Внутрішня мова, універсальний об'єкт, принцип симуляції

**Частина IV. SUBIT_∞ — рекурсивний нескінченний простір**
13. Простір станів S_∞ як дерева з мітками
14. Динаміка й категорія на рівні S_∞
15. Семантична ультраметрика d_Ω
16. Метатеоретичні властивості
17. Відкриті питання
18. Канонічна побудова P(τ): від траєкторії до класифікованої множини

**Частина V. Застосування**
19. Епістемологічний статус і протокол застосування

Глосарій. Додаток A: гексаграмна нотація. Додаток B: коректність конструкції S_∞.

---

# ЧАСТИНА I. SUBIT-64 — алфавіт станів

## 1. Алфавіт примітивів

В основі системи лежить скінченна множина примітивних маркерів потужності 4. Специфікація параметризована вибором конкретної реалізації цієї множини; визначено дві стандартні, взаємно ізоморфні реалізації.

**Семантична реалізація** — для прикладного моделювання конкретних емпіричних систем:

```
A_WHO   = { ME, WE, YOU, THEY }
A_WHERE = { EAST, SOUTH, WEST, NORTH }
A_WHEN  = { SPRING, SUMMER, AUTUMN, WINTER }
```

**Логічна реалізація** — білаттис Белнапа, для формально-логічного трактування:

```
𝔸 = { T, F, B, N }
```

де T = True (Yang), F = False (Yin), B = Both (діалетеїчне значення), N = Neither (паракомплетне значення). Множина 𝔸 несе структуру білаттиса з двома незалежними частковими порядками:

```
порядок істинності:  F <ₜ N <ₜ T,   F <ₜ B <ₜ T
порядок інформації:   N <ᵢ F <ᵢ B,   N <ᵢ T <ᵢ B
```

Кожне значення обох реалізацій кодується двома бітами (bigram = b₁b₂):

| Bigram | Семантична реалізація | Логічна реалізація |
|---|---|---|
| 10 | ME / EAST / SPRING | T |
| 11 | WE / SOUTH / SUMMER | B |
| 01 | YOU / WEST / AUTUMN | F |
| 00 | THEY / NORTH / WINTER | N |

Відповідність між реалізаціями є ізоморфізмом множин потужності 4: він дозволяє переносити алгебраїчні результати між реалізаціями, не встановлюючи жодного змістовного ототожнення на кшталт "ME означає True" — це технічна, а не інтерпретаційна еквівалентність.

## 2. Простір станів S₀

**Означення 2.1.** Простір станів SUBIT-64 визначається як

```
S₀ = A_WHO × A_WHERE × A_WHEN  (або 𝔸³ у логічній реалізації)
|S₀| = 4³ = 64
```

Кожен стан — трійка s = (α, β, γ), де α ∈ A_WHO, β ∈ A_WHERE, γ ∈ A_WHEN. Стан кодується 6-бітним словом b₁b₂b₃b₄b₅b₆, де b₁b₂ кодує WHO, b₃b₄ — WHERE, b₅b₆ — WHEN.

**Гексаграмна нотація.** Розбивши код на дві тригами — Lower Trigram (b₁b₂b₃) і Upper Trigram (b₄b₅b₆) — кожному стану можна зіставити унікальну гексаграму І-Цзін (повна таблиця відповідності — Додаток A). Ця нотація використовується виключно як компактний, однозначний комбінаторний запис 6-бітного слова; жодне додаткове тлумачення традиційної символіки триграм не є частиною формальної специфікації.

**Властивості рівня.** S₀ є статичним простором: відсутній оператор переходу, відсутня внутрішня динаміка, класифікатор істини тривіальний. SUBIT-64 — необхідний алфавіт архетипів, але сам по собі не описує зміну, причинність чи перехід між станами.

---

# ЧАСТИНА II. SUBIT⁺ — скінченна самореферентна динаміка

## 3. Простір правил і розширений стан

**Означення 3.1.** Простір правил еволюції на цьому рівні — скінченна множина

```
ℛ = { ρ₁, ρ₂, …, ρₙ },   ρᵢ : S₀ → S₀
```

**Означення 3.2.** Розширеним станом називається пара

```
ŝ = (s, ρ) ∈ S₀ × ℛ
```

де ρ — активне правило еволюції, що належить самому стану. Це — принципова відмінність SUBIT⁺ від класичного скінченного автомата: у автоматі функція переходу δ зовнішня щодо стану q і фіксується раз і назавжди; у SUBIT⁺ правило ρ є компонентою стану ŝ і само підлягає зміні.

## 4. Оператор еволюції та ієрархія рівнів самореференції

**Означення 4.1.** Оператор еволюції

```
F : S₀ × ℛ → S₀ × ℛ
F(s, ρ) = ( f_ρ(s), g(ρ, s) )
```

де f_ρ : S₀ → S₀ — застосування поточного правила до стану; g : ℛ × S₀ → ℛ — функція метаеволюції, що визначає, як саме правило змінюється залежно від поточного правила й стану.

**Аксіома 4.1 (детермінованість).** F — тотальна функція, визначена для всіх пар (s, ρ) ∈ S₀ × ℛ.

Ця конструкція виявляє чотири рівні самореференції, недоступні класичному автомату:

```
Рівень 0:   s = (α, β, γ)      базовий стан (без правила)
Рівень 1:   ŝ = (s, ρ)         стан із внутрішнім правилом
Рівень 2:   F(ŝ) = (s′, ρ′)    еволюція стану і правила за один крок
Рівень 3:   g(ρ, s) → ρ′       метаеволюція: зміна способу зміни
```

Класичний автомат має лише Рівень 0 і фіксований (незмінний) Рівень 2; SUBIT⁺ додає Рівні 1 і 3, стираючи межу між даними, програмою й метапрограмою.

## 5. Траєкторії у скінченному просторі

**Означення 5.1.** Траєкторією розширеного стану ŝ₀ називається послідовність

```
τ(ŝ₀) = (ŝ₀, ŝ₁, ŝ₂, …),   ŝₙ₊₁ = F(ŝₙ)
```

**Твердження 5.1 (скінченна періодичність).** Оскільки S₀ × ℛ — скінченна множина, а F — детермінована функція цієї множини в саму себе, для будь-якого ŝ₀ існують m ≥ 0 і k ≥ 1 такі, що ŝ_{m+k} = ŝ_m. Іншими словами, кожна траєкторія в SUBIT⁺ рано чи пізно входить у цикл, можливо, після скінченного передперіодичного "хвоста" (принцип Діріхле, застосований до відображення скінченної множини в себе).

Це дає повний перелік типів одноточкових траєкторій, реалізованих на цьому рівні:

| Тип | Умова |
|---|---|
| Атрактор (нерухома точка) | F(ŝ) = ŝ ⟺ f_ρ(s) = s і g(ρ, s) = ρ |
| Орбіта (цикл довжини k > 1) | Fᵏ(ŝ) = ŝ і ∀i < k: Fⁱ(ŝ) ≠ ŝ |
| Передперіодична траєкторія | ∃m > 0, k ≥ 1: ŝ_{m+k} = ŝ_m, а ŝ₀, …, ŝ_{m−1} — без повторів |

Жодна одноточкова траєкторія на цьому рівні не може бути неперіодичною в строгому сенсі — генуїнно неперіодична динаміка вимагає нескінченного простору станів чи правил (Частина IV).

**Алгоритм 5.1 (класифікація за скінченним наближенням).**

```
Вхід: ŝ₀ = (s₀, ρ₀), горизонт пошуку N_max
visited ← ∅
для n = 0, 1, …, N_max:
    якщо ŝₙ ∈ visited:
        L ← n − позначку входження ŝₙ до visited
        якщо L = 1: повернути "атрактор"
        інакше:      повернути "цикл довжини L"
    додати ŝₙ до visited
    ŝₙ₊₁ ← F(ŝₙ)
якщо N_max вичерпано без повтору:
    повернути "цикл довший за N_max" (горизонт пошуку недостатній)
```

---

# ЧАСТИНА III. SUBIT-TOPOS — категорія, класифікатор, типи

## 6. Класифікатор стабільності Ω

Означення й результати цієї Частини формулюються для довільної множини P ⊆ S₀ × ℛ (а не лише для одноточкової траєкторії, як у Частині II), і залишаються дослівно чинними при заміні S₀ на S_∞ у Частині IV.

**Означення 6.1.** Клас стабільності множини P визначається такими умовами:

```
STABLE       ⟺  F(P) = P
METASTABLE   ⟺  F(P) ⊊ P                         (строга власна підмножина)
CYCLIC       ⟺  існує мінімальне k > 1: Fᵏ(P) = P,  і F(P) ≠ P
CHAOTIC      ⟺  F(P) ⊄ P   і   ∀k > 1: Fᵏ(P) ≠ P
```

Ці чотири умови взаємовиключні й вичерпні за побудовою. Дійсно: якщо F(P) ⊆ P і F(P) ≠ P (тобто P метастабільна), то за індукцією Fᵏ(P) ⊆ F(P) ⊊ P для всіх k ≥ 1, тож Fᵏ(P) ≠ P за жодного k — METASTABLE і CYCLIC автоматично несумісні. Якщо ж P задовольняє умову CYCLIC (∃k>1: Fᵏ(P)=P, F(P)≠P), то з щойно наведеного випливає, що F(P) не може бути власною підмножиною P, тобто F(P) ⊄ P — отже, коли F(P) ≠ P, рівно одна з трьох можливостей (METASTABLE, CYCLIC чи CHAOTIC) реалізується, залежно від того, чи F(P) ⊊ P, чи існує k>1 з Fᵏ(P)=P, чи жодне з двох. CHAOTIC, таким чином, є точним доповненням об'єднання трьох інших класів.

**Означення 6.2 (семантична істина).** Множина станів P вважається істинною тоді й лише тоді, коли вона стабільна: P істинне ⟺ F(P) = P. Це — внутрішній критерій: він не апелює до жодного зовнішнього спостерігача, а випливає безпосередньо з динаміки F.

**Зауваження (математична межа скінченного рівня).** Оскільки на рівні SUBIT-64/SUBIT⁺ простір S₀ × ℛ скінченний, а кожна одноточкова траєкторія обов'язково зрештою періодична (Твердження 5.1), клас CHAOTIC для одноточкових траєкторій на цьому рівні не реалізується. Категорія CHAOTIC стає математично змістовною лише для множин P у нескінченному просторі S_∞ (Частина IV).

**Важливе застереження, деталізоване в розділі 18.** Означення 6.1 сформульоване для *вже заданої* множини P. Специфікація до розділу 18 не фіксувала, як саме з довільної спостереженої траєкторії τ отримати множину P, до якої це означення застосовується, — цю прогалину закриває розділ 18.

## 7. Алгебра Ω

На множині Ω = {STABLE, METASTABLE, CYCLIC, CHAOTIC} визначено лінійний порядок:

```
CHAOTIC < CYCLIC < METASTABLE < STABLE
```

Для будь-якого лінійно впорядкованого скінченного ланцюга операції ∧ = min і ∨ = max автоматично утворюють дистрибутивну ґратку. Разом із релятивним псевдодоповненням, визначеним стандартною формулою для ланцюгів (a → b := найбільший елемент ⊤, якщо a ≤ b; інакше b), ця структура утворює **алгебру Гейтінга** (алгебру Ґеделя — лінійну модель інтуїціоністської логіки).

**Кон'юнкція (∧ = min):**

| ∧ | STABLE | METASTABLE | CYCLIC | CHAOTIC |
|---|---|---|---|---|
| **STABLE** | STABLE | METASTABLE | CYCLIC | CHAOTIC |
| **METASTABLE** | METASTABLE | METASTABLE | CYCLIC | CHAOTIC |
| **CYCLIC** | CYCLIC | CYCLIC | CYCLIC | CHAOTIC |
| **CHAOTIC** | CHAOTIC | CHAOTIC | CHAOTIC | CHAOTIC |

**Диз'юнкція (∨ = max):**

| ∨ | STABLE | METASTABLE | CYCLIC | CHAOTIC |
|---|---|---|---|---|
| **STABLE** | STABLE | STABLE | STABLE | STABLE |
| **METASTABLE** | STABLE | METASTABLE | METASTABLE | METASTABLE |
| **CYCLIC** | STABLE | METASTABLE | CYCLIC | CYCLIC |
| **CHAOTIC** | STABLE | METASTABLE | CYCLIC | CHAOTIC |

**Заперечення.** Визначаючи ¬a := a → CHAOTIC і застосовуючи формулу релятивного псевдодоповнення: якщо a = CHAOTIC, то a ≤ ⊥, звідки ¬CHAOTIC = ⊤ = STABLE. Для будь-якого іншого a маємо a > ⊥, тож ¬a = ⊥ = CHAOTIC:

| a | ¬a |
|---|---|
| STABLE | CHAOTIC |
| METASTABLE | CHAOTIC |
| CYCLIC | CHAOTIC |
| CHAOTIC | STABLE |

**Твердження 7.1 (недосяжність класичного виключеного третього).**

```
METASTABLE ∨ ¬METASTABLE = METASTABLE ∨ CHAOTIC = METASTABLE ≠ STABLE
```

Закон виключеного третього не виконується — Ω_SUBIT є моделлю інтуїціоністської, а не класичної (булевої) логіки.

## 8. Категорія SUBIT

**Означення 8.1 (об'єкти).** Об'єктом категорії SUBIT є система X = (S_X, ℛ_X, F_X, g_X), де S_X ⊆ S₀ (у цій Частині; S_X ⊆ S_∞ у Частині IV), ℛ_X ⊆ ℛ, з аксіомою замкненості F_X(S_X × ℛ_X) ⊆ S_X × ℛ_X.

**Означення 8.2 (морфізми).** Морфізм φ: X → Y — пара відображень φ = (φ_S, φ_ℛ), що задовольняє умову динамічної сумісності:

```
φ_S( π_S( F_X(ŝ) ) ) = π_S( F_Y( φ_S(π_S(ŝ)), φ_ℛ(π_ℛ(ŝ)) ) )
```

тобто застосування еволюції з наступним відображенням дає той самий результат, що й відображення з наступною еволюцією. Композиція та тотожні морфізми визначаються покомпонентно; асоціативність і нейтральність тотожного морфізма спадкуються безпосередньо від властивостей композиції функцій. SUBIT — категорія.

**Спеціальні об'єкти й конструкції:**

- *Термінальний об'єкт* **1**: S₁ = {∗}, ℛ₁ = {id}. Для будь-якого X існує рівно один морфізм X → 1.
- *Добуток* X × Y: S_{X×Y} = S_X × S_Y, ℛ_{X×Y} = ℛ_X × ℛ_Y, F_{X×Y}((s₁,s₂),(ρ₁,ρ₂)) = (F_X(s₁,ρ₁), F_Y(s₂,ρ₂)).
- *Експоненціал* Yˣ: S_{Yˣ} — множина динамічно сумісних відображень S_X → S_Y, з оператором евалюації F_{Yˣ}(φ)(s) = F_Y(φ(F_X⁻¹(s))).

**Зауваження про застосовність експоненціала.** Наведена конструкція спирається на існування оберненого відображення F_X⁻¹, тобто застосовна лише до об'єктів X із оборотною динамікою. Для об'єктів із необоротною динамікою ця конструкція не визначена, і статус декартової замкненості категорії SUBIT у повному обсязі є відкритим питанням (розділ 17); вона встановлена лише для повної підкатегорії об'єктів з оборотним F.

## 9. SUBIT як proto-topos

Категорія є топосом, якщо вона має (T1) скінченні ліміти, (T2) експоненціали й (T3) субоб'єктний класифікатор. SUBIT задовольняє (T1) безумовно й (T2) на підкатегорії оборотних об'єктів (розділ 8). Умова (T3) виконується в динамічній, а не класичній формі: замість двозначного Ω = {⊤, ⊥} — Ω_SUBIT = {STABLE, METASTABLE, CYCLIC, CHAOTIC}, що за розділом 7 утворює алгебру Гейтінга, а не булеву алгебру. SUBIT є **proto-topos**. Чи є SUBIT топосом у повністю формальному категоріальному сенсі, залишається відкритим питанням (розділ 17).

## 10. Динамічна теорія типів

**Судження.** Γ ⊢ t : A @ τ означає: у контексті Γ стан t має тип A на фазі еволюції τ. Тип A трактується не як статична класифікація, а як клас траєкторій зі спільним режимом стабільності: A = {ŝ | stability_class(ŝ) = mode_A}.

**Правила виведення:**

```
Рефлексивність:    ───────────────────
                   Γ, x:A@τ ⊢ x : A@τ

Еволюція типу:     Γ ⊢ t:A@τ     F(A) ⊆ B
                   ──────────────────────────
                   Γ ⊢ F(t) : B @ (τ+1)

Стабільний тип:    Γ ⊢ t:A@τ     F(A) = A
                   ──────────────────────────
                   Γ ⊢ t : □A @ τ

Потенційний тип:   Γ ⊢ t:A@τ     ∃k: Fᵏ(A) ⊆ B
                   ─────────────────────────────
                   Γ ⊢ t : ◇B @ τ

Циклічний тип:     Γ ⊢ t:A@τ     ∃k>1: Fᵏ(A) = A
                   ──────────────────────────────
                   Γ ⊢ t : ◯A @ τ
```

Умова циклічного типу обмежена k>1, узгоджено з означенням модальності ◯ у розділі 11 і з означенням CYCLIC у розділі 6.1.

**Атракторна рівність.** Замість класичної ідентичності вводиться відношення s ~_F t ⟺ ∃k: Fᵏ(s) = Fᵏ(t): два стани еквівалентні, якщо їхні траєкторії зрештою збігаються.

## 11. Модальна логіка

| Модальність | Визначення | Змістовний сенс |
|---|---|---|
| □A | F(A) = A | стабільна (необхідна) істина: A — точний F-інваріант |
| ◇A | ∃k ≥ 0: Fᵏ(s) ∈ A | потенційна стабілізація: існує момент входження в A |
| ◯A | ∃k > 1: Fᵏ(s) ∈ A і Fᵏ(A) = A | справжнє циклічне повернення (з періодом, більшим за тривіальний) |

**Аксіоми:**

```
(M1)  □A → A
(M2)  □A → □□A
(M3)  □(A → B) → (□A → □B)
(M4)  ◇A ↔ ¬□¬A
(M5)  ◯A → ◇A
(M6)  □A → ◯A
(M7)  ◯◯A ↔ ◯A
(M8)  □A ∧ □B → □(A ∧ B)
```

*Обґрунтування M6.* Якщо F(A)=A (тобто □A), то застосувавши F ще раз, F²(A)=F(A)=A — отже, ∃k=2>1 такий, що Fᵏ(A)=A, тобто ◯A виконується.

Не виконуються ◇A ∨ ◇¬A і □A ∨ □¬A. Відповідність класифікатору Ω: stable ↔ □A; metastable ↔ A ∧ ¬□A; cyclic ↔ ◯A ∧ ¬□A; chaotic ↔ ¬◇A.

## 12. Внутрішня мова, універсальний об'єкт, принцип симуляції

**Внутрішня мова.** Примітивні терми: s, t, u — терми типу State; ρ, σ — терми типу Rule; p, q — терми типу Prop. Конструктори: who(s), where(s), when(s) — проекції на координати стану; rule(ŝ) — витягування правила зі стану; evolve(ŝ) = F(ŝ). Пропозиційні конструктори: stable(p), reaches(s, A), cycle(A), morphism(φ, X, Y), iso(φ).

**Універсальний об'єкт.** U = (S₀ × ℛ*, F*, g*), де ℛ* — коди всіх допустимих локальних правил, F* — інтерпретатор, що приймає код (s, ρ) системи X і обчислює f_ρ(s), g* — оператор переписування правил. Для будь-якого X ∈ SUBIT постулюється існування морфізму φ_X: U → X. Конструкція кодування encode_X, encode_R подається на рівні принципу.

**Принцип симуляції.** Будь-який скінченний автомат (Q, δ) вкладається в SUBIT⁺: обираємо ін'єкцію ι: Q → S₀; для кожного символу a ∈ Σ будуємо правило ρ_a ∈ ℛ таке, що f_{ρ_a}(ι(q)) = ι(δ(q, a)); визначаємо метаеволюцію g: ρ_a ↦ ρ_{наступний символ}. Наслідок: SUBIT⁺ є умовно універсальною симуляційною системою.

---

# ЧАСТИНА IV. SUBIT_∞ — рекурсивний нескінченний простір

## 13. Простір станів S_∞ як дерева з мітками

**Означення 13.1.** Рекурсивний простір станів визначається як найбільша коалгебраїчна нерухома точка (кінцева коалгебра) функтора `Φ(X) = A ⊔ X³` на категорії множин:

```
S_∞ = νX. (A ⊔ X × X × X)
```

з відповідним відображенням-структурою (out-map)

```
out : S_∞ → A ⊔ S_∞³
```

Цей функтор має два конструктори: **лист**, помічений елементом алфавіту A, і **внутрішній вузол** — трійка піддерев. Явно: S_∞ — множина всіх дерев (скінченних і нескінченних, глибини не більше ω), у яких кожен вузол або є листом із міткою з A, або є внутрішнім вузлом із рівно трьома піддеревами; ототожнення за бісимуляцією (у сенсі антифундаційної аксіоми, Aczel, 1988) допускає нескінченні й циклічні, самореферентні структури серед внутрішніх вузлів.

**Чому потрібен доданок A.** Функтор без базового (листкового) конструктора, Φ'(X) = X³, має кінцеву коалгебру, ізоморфну одноелементній множині (доведення — Додаток B): без жодної мітки немає жодної ознаки, за якою можна відрізнити одне нескінченне саморозгортання дерева від іншого, і всі вони ототожнюються бісимуляцією в єдину точку. Додавання підсумовуваного компонента A вносить генуїнну розрізняльність на кожному рівні розгортання.

**Вкладення S₀ ⊂ S_∞.** Означимо

```
S₀ := { (a, b, c) ∈ S_∞ | a, b, c ∈ A ⊂ S_∞ }
```

Тоді S₀ — підмножина S_∞, що складається з дерев глибини рівно 1, |S₀|=64, узгоджено з Частиною I. **SUBIT-64 є скінченною апроксимацією глибини 1 об'єкта S_∞.**

**Простір правил.** У загальному випадку

```
ℛ = { f : S_∞ → S_∞ | f обчислювана в розширеному сенсі (задана скінченним описом) }
```

ℛ тут може бути нескінченною множиною, і сам S_∞ є нескінченним уже за побудовою. Разом це знімає обмеження Твердження 5.1: генуїнно неперіодична (chaotic у строгому сенсі) динаміка стає математично можливою.

## 14. Динаміка й категорія на рівні S_∞

Оператор еволюції F: S_∞×ℛ→S_∞×ℛ, F(s,ρ)=(f_ρ(s),g(ρ,s)) — формально ідентичний до Частини II, але тепер визначений на нескінченному S_∞. Усі означення розділів 6–11 переносяться дослівно із заміною S₀ на S_∞.

**Множина атракторів.** Fix(F) = {ŝ ∈ S_∞ × ℛ | F(ŝ) = ŝ} є найменшою STABLE-множиною відносно F.

**Категорія.** Означення 8.1–8.2 переносяться дослівно із заміною S_X ⊆ S₀ на S_X ⊆ S_∞. Застереження щодо оборотності F_X в конструкції експоненціала лишається чинним і на цьому рівні.

**Універсальний об'єкт (повна версія).** U = (S_∞ × ℛ*, F*, g*); для будь-якого X ∈ SUBIT постулюється морфізм φ_X: U → X через кодування encode_X: S_X → S_∞, encode_R: ℛ_X → ℛ*.

## 15. Семантична ультраметрика d_Ω

**Означення 15.1 (перша розбіжність).** Для двох розширених станів ŝ, t̂ ∈ S_∞ × ℛ визначимо

```
n₀(ŝ, t̂) := min{ n ≥ 0 : Ω(Fⁿŝ) ≠ Ω(Fⁿt̂) },   якщо таке n існує
```

і покладемо

```
d_Ω(ŝ, t̂) := 2^(−n₀(ŝ,t̂)),  якщо n₀ визначено;   d_Ω(ŝ, t̂) := 0,  якщо Ω(Fⁿŝ) = Ω(Fⁿt̂) для всіх n ≥ 0
```

**Теорема 15.1 (властивості ультраметрики).** Функція d_Ω задовольняє: (1) d_Ω(ŝ, t̂)=0 тоді й лише тоді, коли Ω(Fⁿŝ)=Ω(Fⁿt̂) для всіх n≥0; (2) симетричність; (3) сильну нерівність трикутника d_Ω(ŝ,û)≤max(d_Ω(ŝ,t̂),d_Ω(t̂,û)).

*Доведення (3).* Нехай n₁=n₀(ŝ,t̂), n₂=n₀(t̂,û). Для всіх n<min(n₁,n₂) маємо Ω(Fⁿŝ)=Ω(Fⁿt̂) і Ω(Fⁿt̂)=Ω(Fⁿû), звідки за транзитивністю Ω(Fⁿŝ)=Ω(Fⁿû). Отже n₀(ŝ,û)≥min(n₁,n₂), звідки d_Ω(ŝ,û)≤max(d_Ω(ŝ,t̂),d_Ω(t̂,û)). ∎

**Зауваження (псевдометричність).** d_Ω, строго кажучи, є псевдоультраметрикою на S_∞×ℛ; вона стає справжньою ультраметрикою на факторпросторі (S_∞×ℛ)/~_Ω.

**Теорема 15.2 (нульвимірність).** Простір ((S_∞×ℛ)/~_Ω, d_Ω) є тотально незв'язним; топологія — нульвимірна.

**Важливе застереження, деталізоване в розділі 18.** Означення 15.1 вживає вираз Ω(Fⁿŝ) — застосування Ω до окремого стану, хоча Означення 6.1 визначає Ω лише для множин P. Розділ 18 явно закриває цю прогалину.

## 16. Метатеоретичні властивості

**Несуперечність.** У скінченній апроксимації (Частини I–III) усі аксіоми виконуються в скінченній моделі. Для повного S_∞ несуперечність спадкує від коректності побудови кінцевої коалгебри функтора A⊔X³ (Додаток B).

**Аналогія з неповнотою.** Якщо ℛ достатньо багата, щоб містити правила самоопису системи, правдоподібно очікувати — за аналогією з теоремами Гьоделя — існування тверджень внутрішньої мови, істинних (F(P)=P), але не виведених синтаксично. Це евристична аналогія, а не доведена теорема.

**Ієрархія виразності:**

| Рівень | Додає | Головне обмеження |
|---|---|---|
| SUBIT-64 | статичний алфавіт 64 станів | відсутня динаміка |
| SUBIT⁺ | правило ρ як частина стану; F, g | лише одноточкові траєкторії; CHAOTIC недосяжний |
| SUBIT-TOPOS | класифікатор множин Ω; категорія; типи; модальна логіка | простір станів усе ще скінченний; CCC — умовний |
| SUBIT_∞ | нескінченний рекурсивний S_∞; змістовний клас CHAOTIC; ультраметрика d_Ω | зв'язок Ω з окремою траєкторією не був явним до розділу 18 |

**Відношення до відомих систем:**

| Система | Відношення до SUBIT_∞ |
|---|---|
| Скінченні автомати (DFA/NFA) | Вкладаються через принцип симуляції (розділ 12) |
| Класична (булева) логіка | Спеціальний випадок при вирожденні Ω до {⊤, ⊥} |
| Лінійна (Ґеделева) інтуїціоністська логіка | Реалізується через Ω-алгебру Гейтінга (розділ 7) |
| Лямбда-числення | U — λ-інтерпретатор (розділ 12), F ↔ β-редукція |
| Класичні топоси (Гротендік) | Узагальнення: SUBIT — proto-topos з умовним T2 |
| Білаттис Белнапа (FOUR) | Логічна реалізація базового алфавіту (розділ 1) |
| Метрики на просторах послідовностей / p-адичні метрики | Структурна модель для d_Ω (розділ 15) |
| Коіндукція; регулярні дерева з мітками (`μX. A + X³`) | Конструкція S_∞ (розділ 13, Додаток B) |
| Символічна динаміка, породжувальні розбиття | Крок 0 протоколу застосування (розділ 18.7 → 19.7) |

## 17. Відкриті питання

- Чи існує конструкція експоненціала, застосовна до об'єктів із необоротною динамікою F_X, що робить категорію SUBIT-∞ декартово замкненою в повному обсязі?
- Чи є Ω_SUBIT дійсно внутрішнім субоб'єктним класифікатором у строгому категоріальному сенсі?
- Чи можна строго закодувати арифметику всередині SUBIT_∞ так, щоб аналогія з неповнотою (розділ 16) стала доведеною теоремою?
- **Гіпотеза фрактальної межі.** Чи утворює множина правил ρ, для яких траєкторія фіксованого s₀ є CHAOTIC, множину дробової Хаусдорфової розмірності відносно природної топології на ℛ?
- **Гіпотеза нерухомої точки інтерпретації.** Чи існує розширений стан ŝ* такий, що F(ŝ*) = ŝ* для довільної достатньо багатої ℛ?

## 18. Канонічна побудова P(τ): від траєкторії до класифікованої множини

### 18.1 Постановка задачі

Означення 6.1 (класифікатор Ω) і Означення 15.1 (метрика d_Ω, що вживає Ω(Fⁿŝ)) разом залишають незамкненою одну передумову: як від спостереженої траєкторії τ(ŝ₀)=(ŝ₀,ŝ₁,…) перейти до множини P⊆S_∞×ℛ, яку класифікує Ω. Потрібна функція

```
P : Траєкторії(S_∞×ℛ) → 𝒫(S_∞×ℛ) ∪ {⊥}
```

що задовольняє три вимоги: **(У1, узгодженість)** — на скінченному рівні відтворює типологію Твердження 5.1; **(У2, інваріантність)** — не залежить від довільних параметрів спостереження; **(У3, явність відмови)** — випадок нелокалізованої асимптотичної поведінки позначається явно (⊥), а не мовчки потрапляє в один із чотирьох класів.

### 18.2 Незалежна від Ω топологія на S_∞

Побудова P(τ) не може спиратися на d_Ω (вона сама залежить від Ω — це було б колом). Означимо структурну ультраметрику безпосередньо на деревах:

```
d_S(s,t) := 2^(−δ(s,t))
```

де δ(s,t) — найменша глибина розходження піддерев out(s), out(t) (розділ 13); δ=0, якщо s,t — листки з різними мітками або один із них лист, а інший — вузол; δ(s,t):=1+min(δ(w,w′),δ(x,x′),δ(y,y′)) для вузлів (w,x,y), (w′,x′,y′).

**Твердження 18.1.** d_S — ультраметрика на S_∞, і (S_∞, d_S) — компактний простір.

*Доведення (ескіз).* Ультраметричність — тим самим аргументом "перша розбіжність", що й Теорема 15.1, застосованим до структури дерева. Компактність: S_∞ з цією метрикою гомеоморфний добутковому простору скінченного розгалуження над скінченним алфавітом; за лемою Кеніга (König, 1927) кожна нескінченна послідовність вкладених непорожніх скінченних множин вибору має нескінченний шлях, а компактність такого добуткового простору — окремий випадок теореми Тихонова (Tychonoff, 1930) для зліченного добутку скінченних дискретних просторів. ∎

### 18.3 ω-границя і чому вона завжди непорожня

**Означення 18.1.** ω(ŝ₀) := {ŝ∈S_∞×ℛ | ∃ підпослідовність (nₖ): ŝ_{nₖ}→ŝ у метриці d_S×d_S}.

**Твердження 18.2 (непорожність за компактністю).** Якщо F неперервний відносно d_S (виконується для будь-якого f_ρ, заданого скінченним описом), то ω(ŝ₀) непорожня для довільного ŝ₀.

*Доведення.* Пряме застосування теореми Больцано–Вейєрштрасса до компактного простору (Твердження 18.1). ∎

**Наслідок 18.1.** У самому S_∞ випадок "ω-границя не існує" неможливий за побудовою: компактність гарантує принаймні одну точку накопичення. Вимога У3 тому реалізується не через порожність ω(ŝ₀) (не може статися), а через окрему причину (розділ 18.6).

### 18.4 Чому "P(τ):=ω(ŝ₀)" — неправильна відповідь

**Твердження 18.3 (точна інваріантність ω-границі).** F(ω(ŝ₀)) = ω(ŝ₀) (двобічна рівність).

*Доведення.* (⊇) Нехай y∈ω(ŝ₀): ŝ_{nₖ}→y. За компактністю підпослідовність (ŝ_{nₖ−1}) має збіжну підпідпослідовність до z∈ω(ŝ₀). З неперервності F: F(z)=lim F(ŝ_{nₖ−1})=lim ŝ_{nₖ}=y, тож y∈F(ω(ŝ₀)). (⊆) Якщо y∈ω(ŝ₀), ŝ_{nₖ}→y, то F(ŝ_{nₖ})=ŝ_{nₖ+1}→F(y) за неперервністю, і ŝ_{nₖ+1} — теж підпослідовність τ, тож F(y)∈ω(ŝ₀). ∎ *(Стандартний результат топологічної динаміки; Walters, 1982.)*

**Наслідок 18.2 (колапс класифікатора).** Якщо P(τ):=ω(ŝ₀), умова F(P)=P (означення STABLE) виконується завжди — Ω(P(τ))=STABLE для будь-якої траєкторії за будь-яких обставин. Ця побудова відхиляється: вона задовольняє У1–У3 формально, але робить Ω константним. Задача 18.1 вимагає додаткової вимоги **(У0): P(τ) не повинна апріорно визначати відповідь Ω(P(τ))**.

### 18.5 Коректна побудова: розділення "ансамблю" й "точки на ансамблі"

**Означення 18.2.**

```
m(ŝ₀) := min{ n ≥ 0 : ŝₙ ∈ ω(ŝ₀) }
P_pt(τ)      := { ŝ_{m(ŝ₀)} }
P_nbhd(τ,ε)  := B_ε(ŝ_{m(ŝ₀)})  в метриці d_S
```

На скінченному рівні m(ŝ₀) — те саме m, що в Твердженні 5.1.

**Твердження 18.4 (узгодженість — У1).** Якщо S_X скінченна, P_pt(τ) і Ω-тест дають ту саму відповідь, що Твердження 5.1: F(ŝ_m)=ŝ_m⟺STABLE⟺"атрактор"; ∃k>1:Fᵏ(ŝ_m)=ŝ_m⟺CYCLIC⟺"орбіта". ∎

**Твердження 18.5 (незалежність — У2).** m(ŝ₀) — власна характеристика τ, обчислена відносно всієї нескінченної траєкторії, а не довжини спостереження N. ∎

**Твердження 18.6 (METASTABLE не тавтологічний для синглтона).** Для P_pt(τ)={ŝ_m} умова METASTABLE неможлива: образ синглтона — синглтон (з тотальності F, Аксіома 4.1), ніколи не власна підмножина одноточкової множини. Тому METASTABLE перевіряється на P_nbhd(τ,ε): STABLE ⟺ F(B_ε(ŝ_m))=B_ε(ŝ_m); METASTABLE ⟺ F(B_ε(ŝ_m))⊊B_ε(ŝ_m) для деякого ε>0. ∎

### 18.6 Формулювання функції P(τ) і випадок ⊥

**Означення 18.3 (підсумкова функція).**

```
P(τ) := 
   якщо m(ŝ₀) обчислюваний за скінченну кількість кроків
      (Алгоритм 5.1 чи аналог для S_∞ з апріорною межею глибини):
         повернути пару (P_pt(τ), P_nbhd(τ,ε)) для класифікації за 18.5;
   інакше:
         повернути ⊥ ("асимптотичний режим не локалізовано в межах ресурсів аналізу")
```

⊥ виникає не з математичної непорожності ω(ŝ₀) (завжди непорожня), а з **обчислювальної недосяжності** m(ŝ₀) для конкретного дослідника з конкретними ресурсами — паралель до різниці між Алгоритмом 5.1 ("цикл довший за N_max") і Твердженням 5.1.

### 18.7 Наслідок для протоколу застосування: Крок 0

Побудова P(τ) передбачає, що емпіричні дані вже подані як траєкторія в S_∞×ℛ — нетривіальний вибір, якщо вихідні дані є неперервним виміром, а не вже дискретною послідовністю подій. Тому протокол застосування (розділ 19.3) доповнюється **Кроком 0**, що передує формулюванню гіпотез: обґрунтуванням розбиття (partition) неперервного простору вимірів на скінченний алфавіт A, через яке дані перетворюються на послідовність символів. Це — точний аналог **породжувального розбиття** в символічній динаміці (Kolmogorov, 1958, 1959; Sinai, 1959; Cornfeld, Fomin & Sinai, 1981; Walters, 1982): невдало обране розбиття може породити позірні цикли чи позірну стабільність, яких не існує в самій неперервній динаміці. Детально Крок 0 сформульовано в розділі 19.3.

---

# ЧАСТИНА V. Застосування

## 19. Епістемологічний статус і протокол застосування

### 19.1 Природа формальних результатів цієї специфікації

Усі теореми й твердження Частин I–IV є твердженнями про саму нотацію. Жодне з них не встановлює й не може встановити, що будь-яка конкретна емпірична система, до якої застосовують апарат SUBIT-∞, дійсно еволюціонує за правилом ρ, яке дослідник обирає для її опису, або що присвоєння конкретній емпіричній множині станів певного Ω-класу є істинним у сенсі відповідності реальності.

### 19.2 Функція апарату в дослідницькій роботі

SUBIT-∞ функціонує як інструмент структурування гіпотез, а не як їх джерело чи підтвердження:

- змушує явно сформулювати конкретний причинний механізм (ρ) там, де наративний виклад залишає його імпліцитним;
- дає компактну мову для розрізнення типів переходу, яка спрямовує пошук конкретної емпіричної ознаки;
- дає кількісну (d_Ω) або якісну (Ω) мову для порівняння того, наскільки й де саме розходяться передбачення конкуруючих гіпотез.

### 19.3 Протокол застосування

**Крок 0 (кодування).** Перш ніж формулювати гіпотези, дослідник повинен явно зафіксувати й обґрунтувати розбиття, через яке емпіричні дані перетворюються на послідовність елементів алфавіту A (розділ 18.7). Для наративних, агентних систем (історичні, соціальні) природним розбиттям зазвичай слугує WHO/WHERE/WHEN (Частина I). Для систем без природного дискретного алфавіту (неперервні фізичні виміри) дослідник повинен: (а) явно застерегти, що результат класифікації Ω може бути артефактом вибору розбиття, а не властивістю самої системи, і (б) перевірити чутливість висновку до вибору розбиття — чи зберігається Ω-клас при його огрубленні чи уточненні.

1. Визначити емпіричну систему та дослідницьке питання.
2. Сформулювати конкуруючі гіпотези як явні, відмінні одне від одного правила ρ₁, ρ₂, …
3. Для кожного ρᵢ вивести емпіричне передбачення, відмінне від передбачень інших гіпотез.
4. Зібрати або розглянути наявні першоджерельні дані, що підтверджують чи спростовують кожне передбачення.
5. Лише після кроку 4 — за потреби — присвоїти Ω-клас переходу (через канонічну побудову P(τ), розділ 18) як компактний підсумковий ярлик уже здобутого емпіричного висновку. Ω-клас ніколи не замінює крок 4.
6. У викладі результатів явно розрізняти: (а) формально доведене в самій нотації; (б) підтверджене конкретним емпіричним джерелом; (в) інтерпретаційна пропозиція дослідника.

---

## Глосарій і зведена таблиця символів

| Символ | Визначення | Рівень |
|---|---|---|
| A, 𝔸 | Базовий алфавіт примітивів, \|A\| = 4 | I |
| S₀ | Скінченний простір станів = A³, \|S₀\| = 64 | I |
| ℛ | Правила еволюції (скінченна в II–III, потенційно нескінченна в IV) | II, IV |
| ŝ = (s, ρ) | Розширений стан | II |
| F | Оператор еволюції, F(s, ρ) = (f_ρ(s), g(ρ, s)) | II |
| f_ρ | Локальна еволюція стану під правилом ρ | II |
| g | Метаеволюція — оператор зміни правила | II |
| τ(ŝ) | Траєкторія, що починається в ŝ | II |
| Fix(F) | Множина атракторів {ŝ \| F(ŝ) = ŝ} | II |
| Ω | Класифікатор {STABLE, METASTABLE, CYCLIC, CHAOTIC} | III |
| □A, ◇A, ◯A | Стабільна істина; потенційна стабілізація; циклічне повернення (k>1) | III |
| ~_F | Атракторна еквівалентність: ∃k: Fᵏ(s) = Fᵏ(t) | III |
| φ, id_X | Морфізм категорії SUBIT; тотожний морфізм | III |
| 1, Yˣ | Термінальний об'єкт; експоненціал (умовний, для оборотних F) | III |
| U | Універсальний об'єкт (внутрішній інтерпретатор) | III, IV |
| CCC | Декартово замкнена категорія (статус — умовний) | III, IV |
| S_∞ | νX.(A ⊔ X³) — простір дерев з мітками з A, можливо нескінченної глибини | IV |
| out | Структурне відображення S_∞ → A ⊔ S_∞³ | IV |
| d_Ω | Семантична (псевдо)ультраметрика за принципом першої розбіжності | IV |
| n₀(ŝ, t̂) | Горизонт узгодженості — перший крок розбіжності Ω-класів | IV |
| d_S | Структурна ультраметрика на S_∞, незалежна від Ω | IV |
| ω(ŝ₀) | ω-границя траєкторії — множина точок накопичення | IV |
| m(ŝ₀) | Момент першого входження траєкторії в її власну ω-границю | IV |
| P(τ), P_pt, P_nbhd(ε) | Канонічна функція траєкторія→множина; точка входження; ε-окіл навколо неї | IV |
| Породжувальне розбиття | Розбиття неперервного простору станів на скінченний алфавіт, що не спотворює символьну динаміку | IV, V |
| Розрізняльний тест | Емпіричне спостереження з різними передбаченнями для конкуруючих гіпотез | V |

---

## Додаток A. Гексаграмна нотація SUBIT-64

Кожному з 64 станів s = (b₁b₂b₃)(b₄b₅b₆) відповідає унікальна гексаграма І-Цзін, від ䷀ (111111) до ䷿ (010101). Ця відповідність використовується виключно як компактна комбінаторна нотація 6-бітного слова.

| № | Гекс. | Код | № | Гекс. | Код | № | Гекс. | Код | № | Гекс. | Код |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 01 | ䷀ | 111111 | 17 | ䷐ | 100110 | 33 | ䷠ | 001111 | 49 | ䷰ | 101110 |
| 02 | ䷁ | 000000 | 18 | ䷑ | 011001 | 34 | ䷡ | 111100 | 50 | ䷱ | 011101 |
| 03 | ䷂ | 100010 | 19 | ䷒ | 110000 | 35 | ䷢ | 000101 | 51 | ䷲ | 100100 |
| 04 | ䷃ | 010001 | 20 | ䷓ | 000011 | 36 | ䷣ | 101000 | 52 | ䷳ | 001001 |
| 05 | ䷄ | 111010 | 21 | ䷔ | 100101 | 37 | ䷤ | 101011 | 53 | ䷴ | 001011 |
| 06 | ䷅ | 010111 | 22 | ䷕ | 101001 | 38 | ䷥ | 110101 | 54 | ䷵ | 110100 |
| 07 | ䷆ | 010000 | 23 | ䷖ | 000001 | 39 | ䷦ | 001010 | 55 | ䷶ | 101100 |
| 08 | ䷇ | 000010 | 24 | ䷗ | 100000 | 40 | ䷧ | 010100 | 56 | ䷷ | 001101 |
| 09 | ䷈ | 111011 | 25 | ䷘ | 100111 | 41 | ䷨ | 110001 | 57 | ䷸ | 011011 |
| 10 | ䷉ | 110111 | 26 | ䷙ | 111001 | 42 | ䷩ | 100011 | 58 | ䷹ | 110110 |
| 11 | ䷊ | 111000 | 27 | ䷚ | 100001 | 43 | ䷪ | 111110 | 59 | ䷺ | 010011 |
| 12 | ䷋ | 000111 | 28 | ䷛ | 011110 | 44 | ䷫ | 011111 | 60 | ䷻ | 110010 |
| 13 | ䷌ | 101111 | 29 | ䷜ | 010010 | 45 | ䷬ | 000110 | 61 | ䷼ | 110011 |
| 14 | ䷍ | 111101 | 30 | ䷝ | 101101 | 46 | ䷭ | 011000 | 62 | ䷽ | 001100 |
| 15 | ䷎ | 001000 | 31 | ䷞ | 001110 | 47 | ䷮ | 010110 | 63 | ䷾ | 101010 |
| 16 | ䷏ | 000100 | 32 | ䷟ | 011100 | 48 | ䷯ | 011010 | 64 | ䷿ | 010101 |

---

## Додаток B. Коректність конструкції S_∞

**Лема B.1.** Кінцева коалгебра функтора Φ'(X) = X × X × X на категорії множин ізоморфна одноелементній множині: νX. X³ ≅ 1.

*Доведення.* Кінцеву коалгебру функтора, що зберігає границі, можна обчислити як границю термінальної послідовності 1←Φ'(1)←Φ'²(1)←…, де 1 — термінальний об'єкт. Оскільки 1×1×1≅1, послідовність стабілізується на першому кроці: Φ'(1)=1³≅1, і так далі. Границя стабільної послідовності дорівнює самій цій точці, тож νX.X³≅1. ∎

**Наслідок B.2.** Будь-яка спроба вкласти алфавіт A потужності більшої за 1 у νX.X³ як листки (ототожнюючи a∈A з розв'язком s=(s,s,s)) зазнає невдачі: усі такі розв'язки бісимулярні між собою й ототожнюються в одну точку — точна відповідність Лемі B.1.

**Лема B.3.** Кінцева коалгебра функтора Φ(X)=A⊔X³ (Означення 13.1) не вироджується в одноелементну множину, якщо |A|>1.

*Доведення (ескіз).* Термінальна послідовність для Φ: X₀=1, X₁=A⊔1³=A⊔1, X₂=A⊔(A⊔1)³,… Оскільки на кожному кроці зберігається копія A як окрема, нескорочувана компонента коопродукту (X⊔1≇1 для |X|>0), послідовність не стабілізується на одноелементній множині; її границя містить принаймні |A| попарно різних елементів. Отже S_∞=νX.(A⊔X³) містить принаймні |A|=4 різних точки, і зокрема містить S₀=A³ як підмножину дерев глибини 1, |S₀|=64. ∎

---

## Підсумкова формула

```
SUBIT-∞ = ( S_∞, ℛ, F, g, Ω, d_Ω, U, ℒ, P ),   S_∞ = νX.(A ⊔ X × X × X)
```

де S_∞ — рекурсивний простір дерев із мітками з A, що містить S₀=A³ (|S₀|=64) як вкладену скінченну апроксимацію глибини 1; ℛ — простір внутрішніх правил еволюції; F — оператор динаміки; g — оператор метадинаміки; Ω — динамічний класифікатор стабільності, що утворює алгебру Гейтінга на чотирьох значеннях; d_Ω — семантична ультраметрика за принципом першої розбіжності; U — універсальний об'єкт-інтерпретатор; ℒ — внутрішня мова динамічної теорії типів і модальної логіки; P — канонічна функція, що зіставляє довільній траєкторії множину, придатну для Ω-класифікації, або явну відмову ⊥.

**Центральна теза.** Правило належить стану. Істина визначається стабільністю: F(P)=P. Відстань між траєкторіями вимірюється горизонтом їхньої першої розбіжності. Категорія прагне до декартової замкненості на підкатегорії оборотних об'єктів. Простір станів — дерево з мітками, а не немарковане дерево. Класифікатор Ω застосовується не до самої траєкторії безпосередньо, а до канонічно побудованої множини P(τ), і ця побудова сама підлягає явним, доведеним обмеженням.

**Центральне методологічне положення.** Формальна коректність цієї нотації не переносить сама по собі жодного емпіричного змісту на будь-яку конкретну систему, описану її мовою: висновок про те, що щось у світі є "stable" чи "chaotic", завжди залишається інтерпретаційним кроком дослідника, який потребує незалежного емпіричного обґрунтування, і який починається з явного обґрунтування самого кодування емпіричних даних у мову SUBIT (Крок 0).

---

## Бібліографія (додатково до джерел попередніх версій)

Cornfeld, I. P., Fomin, S. V., & Sinai, Ya. G. (1981). *Ergodic Theory*. Springer-Verlag.

Kolmogorov, A. N. (1958). New metric invariant of transitive dynamical systems and automorphisms of Lebesgue spaces. *Doklady Akademii Nauk SSSR*, 119, 861–864.

Kolmogorov, A. N. (1959). Entropy per unit time as a metric invariant of automorphisms. *Doklady Akademii Nauk SSSR*, 124, 754–755.

König, D. (1927). Über eine Schlussweise aus dem Endlichen ins Unendliche. *Acta Litterarum ac Scientiarum (Szeged)*, 3, 121–130.

Sinai, Ya. G. (1959). On the notion of entropy of a dynamical system. *Doklady Akademii Nauk SSSR*, 124, 768–771.

Tychonoff, A. (1930). Über die topologische Erweiterung von Räumen. *Mathematische Annalen*, 102, 544–561.

Walters, P. (1982). *An Introduction to Ergodic Theory*. Springer-Verlag.

*(Повний список джерел до Частин I–III і Додатку B — без змін відносно v5.1: Aczel 1988; Belnap 1977a/b; Dunn 1976; Dummett 1959; Gödel 1932; Hensel 1897; Lambek & Scott 1986; Lawvere 1964/1970; Rutten 2000; Smith 1984; Swetz 2003 та ін. — див. супровідні коментарі до кожної Частини.)*

---

*SUBIT-∞ Specification, версія 5.2.*
